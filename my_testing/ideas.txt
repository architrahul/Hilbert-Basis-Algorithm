Covering Design Algorithm

Let the covering number C(n, t, k) denote the smallest number of tsubsets of a n-set that cover all k-subsets.

For a fixed value of k, (k is small) find an optimal t in for which we can apply the covering design algorithm 
as well as the hilbert basis algorithm in minimum time.


maybe we run one iteration of each value of t and then 
since we know the value of C(n, k(the subset size which we're gauranting will be covered), t) we can multiply that time like
 t x C(n, k, t) and find optimized t and run for it.

ToDo Merge pipeline 1 and 2 and add flags